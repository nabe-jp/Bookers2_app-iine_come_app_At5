<div class="container"> 
  <div class="row"> 
    <div class="col-md-3">
      <!-- ユーザー情報 -->
      <%= render 'user_info', user: @user %>

      <!-- 新規投稿作成 -->
      <h3>New book</h3>
      <%= render 'layouts/message', resece: @book %>
      <%= render 'books/book_edit_form', book: @book %>
    </div>

    <div class="col-md-8  offset-md-1">
      <h3>Books</h3>
      <%= render 'books/books_table', books: @books, kaminari: @books %>
    </div>
  </div>
</div>