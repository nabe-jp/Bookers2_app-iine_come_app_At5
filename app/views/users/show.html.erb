<div class="container"> 
  <div class="row"> 
    <div class="col-md-3">
      <%= render 'user_info', user: @user %>
      <%= render 'user_edit', book: @book %>
    </div>
    <div class="col-md-8  offset-md-1">
      <%= render 'books/books_table', books: @books %>

      <!--kaminari用-->
      <%= paginate @books %>
    </div>
  </div>
</div>